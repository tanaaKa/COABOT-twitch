/*
 * Author: tanaKa
 * Initialize the coalition twitch bot to monitor a twitch channel
 *
 * Called from within the mod, locally
 *
 * Arguments:
 * N/A
 *
 * Return Value:
 * STRING - Error or "bot running" message
 *
 * Example:
 * call COABOT_fnc_initBot;
 *
 * Public: Yes
 */

#include "script_component.hpp"
if (!hasInterface) exitWith {};

// Ensure the bot is setup correctly

// Get variables
  
// Call the pythia function
  
// else exit with error